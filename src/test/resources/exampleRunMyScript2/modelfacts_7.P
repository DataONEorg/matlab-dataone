
% FACT: program(program_id, program_name, qualified_program_name, begin_annotation_id, end_annotation_id).
program(1, 'myScript2', 'myScript2', 1, 10).
program(2, 'generate_pi_file', 'myScript2.generate_pi_file', 4, 6).
program(3, 'generate_second_file', 'myScript2.generate_second_file', 7, 9).

% FACT: workflow(program_id).
workflow(1).

% FACT: function(program_id).

% FACT: has_subprogram(program_id, subprogram_id).
has_subprogram(1, 2).
has_subprogram(1, 3).

% FACT: port(port_id, port_type, port_name, qualified_port_name, port_annotation_id, data_id).
port(19, 'out', 'myfile4.nc', 'myScript2->myfile4.nc', 2, 12).
port(20, 'out', 'myncfile4.nc', 'myScript2->myncfile4.nc', 3, 13).
port(21, 'out', 'myfile4.nc', 'myScript2.generate_pi_file->myfile4.nc', 5, 14).
port(22, 'out', 'myncfile4.nc', 'myScript2.generate_second_file->myncfile4.nc', 8, 15).

% FACT: port_alias(port_id, alias).

% FACT: port_uri_template(port_id, uri).

% FACT: has_in_port(block_id, port_id).

% FACT: has_out_port(block_id, port_id).
has_out_port(1, 19).
has_out_port(1, 20).
has_out_port(2, 21).
has_out_port(3, 22).

% FACT: data(data_id, data_name, qualified_data_name).
data(12, 'myfile4.nc', '[myfile4.nc]').
data(13, 'myncfile4.nc', '[myncfile4.nc]').
data(14, 'myfile4.nc', 'myScript2[myfile4.nc]').
data(15, 'myncfile4.nc', 'myScript2[myncfile4.nc]').

% FACT: channel(channel_id, data_id).
channel(13, 12).
channel(14, 13).
channel(15, 14).
channel(16, 15).

% FACT: port_connects_to_channel(port_id, channel_id).
port_connects_to_channel(21, 13).
port_connects_to_channel(22, 14).
port_connects_to_channel(21, 15).
port_connects_to_channel(22, 16).

% FACT: inflow_connects_to_channel(port_id, channel_id).

% FACT: outflow_connects_to_channel(port_id, channel_id).
outflow_connects_to_channel(19, 13).
outflow_connects_to_channel(20, 14).
outflow_connects_to_channel(19, 15).
outflow_connects_to_channel(20, 16).

% FACT: uri_variable(uri_variable_id, variable_name, port_id).
